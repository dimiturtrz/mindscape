{
  "//": "Duplication detector (bd apl) — the DRY axis token/dead-code linters can't see. threshold = max % dup before a nonzero exit. PATHS are passed on the CLI (jscpd resolves a config `path` relative to the config file, which breaks when the config lives under devtools/) — run: `npx jscpd@4 core neuroscan --config devtools/jscpd.json`. ADVISORY in CI (continue-on-error) until (a) the class-extraction refactor (bd ylq) settles the code and (b) duplication is driven under threshold; THEN graduate to blocking as a DRY regression guard. Current python dup ~2.65%.",
  "threshold": 1,
  "minTokens": 50,
  "gitignore": true,
  "ignore": ["**/node_modules/**", "**/dist/**", "**/__pycache__/**", "runs/**", "mlruns/**", "external/**", "neuroviz/web/**"],
  "reporters": ["console"],
  "absolute": false
}
