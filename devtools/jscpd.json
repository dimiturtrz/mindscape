{
  "//": "Duplication detector (bd apl) — the DRY axis token/dead-code linters can't see. threshold = max % dup before a nonzero exit. PATHS are passed on the CLI (jscpd resolves a config `path` relative to the config file, which breaks when the config lives under devtools/) — run: `npx jscpd@4 core neuroscan --config devtools/jscpd.json`. ENFORCED (blocks) in CI: dup is 0.7% < 1% after the runner boilerplate was DRY-extracted (Cli.setup_logging, Riemann.cross_subject_decode). Ratchet the threshold DOWN as dup settles, never up.",
  "threshold": 1,
  "minTokens": 50,
  "gitignore": true,
  "ignore": ["**/node_modules/**", "**/dist/**", "**/__pycache__/**", "runs/**", "mlruns/**", "external/**", "neuroviz/web/**"],
  "reporters": ["console"],
  "absolute": false
}
