# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / lint / type caches
.pytest_cache/
.ruff_cache/
.import_linter_cache/
.grimp_cache/

# archmap: graph.json IS committed (the diffable architecture artifact); the interactive viewer shell is
# regenerated (template-identical, ~700KB of vendored libs) locally / in the Pages workflow — not committed.
docs/architecture/index.html

# Virtualenv
.venv/
venv/
.env

# Local machine config — the data root lives outside the repo
paths.yaml

# Private/local-only docs (generic pattern — reveals no intent in the ignore list itself)
*.local.md
.local/

# Data / weights / artifacts (never commit large or licensed data)
data/raw/
data/processed/
datasets/
# misplaced/relative data roots (paths.yaml bug — data must live OUTSIDE the repo)
D-/
D:/
# neural recordings (EEG/MEG) + arrays + dataset archives
*.edf
*.bdf
*.gdf
*.set
*.fif
*.vhdr
*.eeg
*.mat
*.gz
*.zip
*.npy
*.npz
*.bin
*.pth
*.pt
*.joblib
*.onnx
*.engine
mlflow.db
mlartifacts/
mlruns/
runs/
checkpoints/
logs/
wandb/
# MOABB / MNE download caches (point here via paths.yaml; never commit recordings)
mne_data/
.mne/

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# neuroviz generated topo data (derived from EEG; regenerate via neuroviz.export)
neuroviz/web/data/
elevator.txt
.coverage
.claude/

# Ephemeral session launch/watcher scripts (belong in the scratchpad dir, not the repo)
scratch_*.sh

# external git-repo deps: checked out, pinned, never vendored (see CLAUDE.md)
external/

# devtools gate caches (sdlc-scaffold adoption — bd 588n)
.ruff_cache/
.grimp_cache/
.import_linter_cache/
.pytest_cache/
htmlcov/
